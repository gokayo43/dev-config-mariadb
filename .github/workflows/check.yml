name: Check

# The gate the two MariaDB-bound repos run, called by reference:
#   jobs.check.uses: gokayo43/dev-config-mariadb/.github/workflows/check.yml@<sha>
#
# Everything static is dev-config's, unchanged: this workflow calls that one
# rather than restating any of it, so a gate lands here when the pin below moves
# and never because a copy drifted. The `database: false` in that call is not a
# preference — dev-config's database job is Postgres, and the MariaDB jobs this
# workflow adds are what a consumer gets instead. This workflow's own `database`
# input turns those on and never reaches that call.
on:
  workflow_call:
    inputs:
      # No descriptions on a pass-through, deliberately. dev-config declares
      # every one of these and says what it does, including the conditions under
      # which it refuses one; a second copy of that prose is a copy that drifts,
      # and `description` is optional on a workflow_call input. The two inputs
      # that carry one say what dev-config cannot: a fact about calling this
      # workflow instead of that one, and a fact about a job of this repo's own.
      build:
        type: boolean
        default: false
      affected:
        type: boolean
        default: false
      compose:
        type: boolean
        default: false
      mutation-lane:
        type: boolean
        default: false
      mutation-floor:
        type: string
        default: ""
      contract-exemptions:
        description: >-
          A consumer of this workflow names `ci-call` among these: dev-config's
          repo contract asks for a job calling its own check.yml at a pinned SHA,
          and a consumer's call into it is one level down, where that gate cannot
          read it.
        type: string
        default: ""
      stack-allowlist:
        type: string
        default: ""
      test-network:
        type: string
        default: ""
      test-suite-evidence:
        type: string
        default: ""
      database:
        description: >-
          Replay the repo's own migrations onto an empty MariaDB, twice, compare
          the schema either side, then boot the app against the result, probe it
          where the repo has a probe, and ramp it. This is a job of this repo's
          own and this input never reaches dev-config's check.yml, whose job of
          that name is Postgres.
        type: boolean
        default: false
      db-gate-evidence:
        type: string
        default: ""
      start-command:
        type: string
        default: bun run start
      health-url:
        type: string
        default: http://localhost:3000/api/health
      probe-command:
        type: string
        default: ""
      probe-timeout:
        type: string
        default: ""
      capacity-script:
        type: string
        default: ""
      capacity-path:
        type: string
        default: ""
      route-allowlist:
        type: string
        default: ""

permissions:
  contents: read

jobs:
  static:
    uses: gokayo43/dev-config/.github/workflows/check.yml@602fca2f24341ef9c445b4a6f750e887cf156f37 # v0.50.1
    with:
      build: ${{ inputs.build }}
      affected: ${{ inputs.affected }}
      compose: ${{ inputs.compose }}
      mutation-lane: ${{ inputs['mutation-lane'] }}
      mutation-floor: ${{ inputs['mutation-floor'] }}
      contract-exemptions: ${{ inputs['contract-exemptions'] }}
      stack-allowlist: ${{ inputs['stack-allowlist'] }}
      test-network: ${{ inputs['test-network'] }}
      test-suite-evidence: ${{ inputs['test-suite-evidence'] }}
      # A literal, always. Every other value in this call is a caller's input
      # handed on; this one is the workflow's own answer, and the input of the
      # same name above decides a job here rather than that one. README.md names
      # the inputs that reach neither, and the suite holds that list to what
      # dev-config declares at the pin above.
      database: false

  # Every input aimed at the database job is a request that job would otherwise
  # never see, and a gate nobody notices is off is the worst kind — so a call
  # that asks for one without asking for the job is refused rather than ignored.
  #
  # A job rather than a step, because the only other job that always runs here
  # is a `uses:` and a called workflow takes no steps of a caller's. It has no
  # `if:` for the reason dev-config's own guard has none: a condition restating
  # what the body tests is a second statement of one rule, and the half that
  # gets forgotten is the guard. The body is the rule.
  refusals:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - env:
          DATABASE: ${{ inputs.database }}
          DB_GATE_EVIDENCE: ${{ inputs['db-gate-evidence'] }}
          START_COMMAND: ${{ inputs['start-command'] }}
          HEALTH_URL: ${{ inputs['health-url'] }}
          PROBE_COMMAND: ${{ inputs['probe-command'] }}
          PROBE_TIMEOUT: ${{ inputs['probe-timeout'] }}
          CAPACITY_SCRIPT: ${{ inputs['capacity-script'] }}
          CAPACITY_PATH: ${{ inputs['capacity-path'] }}
          ROUTE_ALLOWLIST: ${{ inputs['route-allowlist'] }}
          # The two inputs above that carry a non-empty default, written out a
          # second time so the guard below has something to compare against.
          # tests/refusals.test.ts holds these two to the defaults declared at
          # the top of this file; a workflow_call input cannot be asked whether
          # the caller passed it, and `github.event.inputs` is not populated for
          # one, so "the caller wrote this" has no other spelling here.
          START_COMMAND_DEFAULT: bun run start
          HEALTH_URL_DEFAULT: http://localhost:3000/api/health
        shell: bash
        run: |
          set -euo pipefail
          wrong=0
          refuse() {
            echo "::error::$1"
            wrong=1
          }
          if [ "$DATABASE" != true ] && [ -n "$DB_GATE_EVIDENCE" ]; then
            refuse "db-gate-evidence needs database: true — it names the artifact the database job uploads, and this call leaves that job off"
          fi
          if [ "$DATABASE" != true ] && [ -n "$PROBE_COMMAND" ]; then
            refuse "probe-command needs database: true — the probe runs against the app the database job boots, and this call leaves that job off"
          fi
          if [ "$DATABASE" != true ] && [ -n "$PROBE_TIMEOUT" ]; then
            refuse "probe-timeout needs database: true — it bounds a probe the database job runs, and this call leaves that job off"
          fi
          if [ "$DATABASE" != true ] && [ -n "$CAPACITY_SCRIPT" ]; then
            refuse "capacity-script needs database: true — the ramp runs against the app the database job boots, and this call leaves that job off"
          fi
          if [ "$DATABASE" != true ] && [ -n "$CAPACITY_PATH" ]; then
            refuse "capacity-path needs database: true — the ramp runs against the app the database job boots, and this call leaves that job off"
          fi
          if [ "$DATABASE" != true ] && [ -n "$ROUTE_ALLOWLIST" ]; then
            refuse "route-allowlist needs database: true — the route floor is measured across the database job's ramp, and this call leaves that job off"
          fi
          # Compared with their defaults rather than tested for emptiness, which
          # is the whole of what these two can be asked. Every other input aimed
          # at the job defaults to "", so non-empty IS "the caller passed it";
          # these two carry a value, because a consumer moving between this
          # workflow and dev-config's writes one call either way and the
          # defaults are theirs. dev-config#66 is the same two inputs going
          # unrefused there, where the guard tests for emptiness alone. What
          # this still cannot see is a caller who passes exactly the default —
          # docs/gates/db-serving.md names that, rather than papering it over.
          if [ "$DATABASE" != true ] && [ "$START_COMMAND" != "$START_COMMAND_DEFAULT" ]; then
            refuse "start-command needs database: true — it is how the database job's boot step starts the app, and this call leaves that job off"
          fi
          if [ "$DATABASE" != true ] && [ "$HEALTH_URL" != "$HEALTH_URL_DEFAULT" ]; then
            refuse "health-url needs database: true — it is what the database job's boot step polls and what its ramp measures, and this call leaves that job off"
          fi
          # Asked whatever the job, because it is a fact about the pair rather
          # than about the job: a bound with no command under it bounds nothing,
          # and an input that reaches a step which never reads it is the class
          # every refusal here exists to kill.
          if [ -z "$PROBE_COMMAND" ] && [ -n "$PROBE_TIMEOUT" ]; then
            refuse "probe-timeout needs probe-command — it bounds that command, and on its own it bounds nothing"
          fi
          exit "$wrong"

  replay:
    if: inputs.database
    runs-on: ubuntu-latest
    timeout-minutes: 15

    # Empty on every run, which is the one state no developer database is ever
    # in. Pinned by digest for the same reason an action is, with the tag kept
    # inline: that is the form Renovate's docker datasource maintains.
    #
    # 11.4 because that is what the server this gate is written for runs —
    # nfp-elysia's, probed at 11.4.12-MariaDB. The other repo named in README.md
    # runs MySQL 8.0.32 rather than MariaDB at all, which is a lane this
    # workflow does not have and issue #9 is where it is decided; until then a
    # consumer on MySQL is refused by this image rather than quietly graded
    # against another server's idea of a schema.
    #
    # Redis is started with it, unconditionally: a repo whose migrator imports
    # its own environment module gets that module's Redis check on the way in,
    # and an idle container costs a couple of seconds.
    services:
      mariadb:
        image: mariadb:11.4@sha256:4f1d8d202fcf7bcb3902f63af09f9c1a050c2922a89652f22abaec0d4f015e83
        env:
          MARIADB_ROOT_PASSWORD: mariadb
          MARIADB_DATABASE: app
        ports:
          - 3306:3306
        # Over TCP on purpose. The image's entrypoint runs a temporary server
        # with networking off while it initialises, so a socket ping answers
        # while the database the job is about to migrate does not exist yet.
        options: >-
          --health-cmd "mariadb-admin --protocol=tcp -h 127.0.0.1 -uroot -pmariadb ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 30
      redis:
        image: redis:7-alpine@sha256:ff02b58f971e7d7d156a1267e283fcbbeee91773b6aa36c49dac28ecfe28eadf
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 30

    # 127.0.0.1 rather than localhost: a MySQL-family client reads that name as
    # an instruction to use a unix socket, and the server is a container.
    env:
      DATABASE_URL: mysql://root:mariadb@127.0.0.1:3306/app
      REDIS_URL: redis://127.0.0.1:6379

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Nothing in this job pushes, and it installs and runs the repo's own
          # dependencies in the same workspace the token would sit in.
          persist-credentials: false

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      - run: bun install --frozen-lockfile

      # The same step dev-config's database job runs, and for the same reason: a
      # repo whose migrator is generated — a schema compiled out of TypeScript, a
      # lineage a build writes — has nothing for this job to replay until the
      # build has run. Without it such a repo is refused for declaring no
      # migrations, which is a false answer to the question this gate asks.
      - if: inputs.build
        run: bun run build

      # By full path and SHA rather than `./`: inside a called workflow a
      # relative `uses:` resolves against the CALLER's checkout, where this
      # repo's actions are not. The consequence is that the action and the
      # workflow pinning it are two commits — this repo's release contract, in
      # CLAUDE.md under "Releasing an action".
      #
      # The image is written twice in this file, in the service above and here,
      # because GitHub gives a service image no way to read a value declared
      # anywhere else. tests/wrapper-inputs.test.ts is what holds the two to one
      # digest.
      - uses: gokayo43/dev-config-mariadb/.github/actions/db-replay@dfb622f1375e813dcedace8551009873e996becc
        with:
          # renovate: datasource=docker depName=mariadb
          db-image: mariadb:11.4@sha256:4f1d8d202fcf7bcb3902f63af09f9c1a050c2922a89652f22abaec0d4f015e83

      # Migrations applying says nothing about whether an app can run against
      # what they built, a route table answering says nothing about whether any
      # answer was right, and neither says anything about what the surface does
      # under load. Same job as the replay above, because the database this
      # boots against is that job's service container and a second job would get
      # an empty one. docs/gates/db-serving.md is the page.
      - uses: gokayo43/dev-config-mariadb/.github/actions/db-serving@0000000000000000000000000000000000000000
        with:
          start-command: ${{ inputs['start-command'] }}
          health-url: ${{ inputs['health-url'] }}
          probe-command: ${{ inputs['probe-command'] }}
          probe-timeout: ${{ inputs['probe-timeout'] }}
          capacity-script: ${{ inputs['capacity-script'] }}
          capacity-path: ${{ inputs['capacity-path'] }}
          route-allowlist: ${{ inputs['route-allowlist'] }}

      # Everything the steps above wrote into the runner, which is deleted with
      # the runner. The upload belongs to the job rather than to either action:
      # one job builds one database and runs one app against it, an artifact
      # name may be claimed once per run, and two actions publishing under the
      # caller's one name would be the second of them failing on the duplicate.
      #
      # A step that failed on the way to a number is exactly when its partial
      # evidence is worth having — the schema a run never got to compare, the
      # summary of a ramp that died, the output of an app that never answered —
      # and a cancelled run has nothing to say. Reproducing any of them by
      # re-running with more printing is the shape of debugging every diagnostic
      # here exists to avoid, so the files leave the run whatever happened to
      # it.
      #
      # server.log is copied while the app still holds it open, which is not a
      # race to fix: the ramp needs the process alive, so there is no moment
      # where the file is finished and the run is not. Read its tail as possibly
      # torn. The boot step's own annotation, not this file, is what says an app
      # failed to start.
      - name: Publish what this job built, measured and read
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          # The one place this name is defaulted, so that a caller passing an
          # empty string through is the same as a caller not asking for one.
          name: ${{ inputs['db-gate-evidence'] || 'db-gate-evidence' }}
          path: |
            ${{ runner.temp }}/replay-from-empty.schema
            ${{ runner.temp }}/replay-again.schema
            ${{ runner.temp }}/server.log
            ${{ runner.temp }}/capacity.json
            ${{ runner.temp }}/route-log-before.json
            ${{ runner.temp }}/route-log-after.json
          # Each of these is written by a step that may not have been reached.
          if-no-files-found: ignore
